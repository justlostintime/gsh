' Gambas class file

Static Property Read max As Integer

Static Public Sub _get(index As Integer) As String
  Dim result As String
  Try result = Highlight.Styles[index]
  If Error Then 
    result = "Unknown"
  Endif
  
  Return result
  
End


Static Private Function max_Read() As Integer

  Return Highlight.Styles.max

End
